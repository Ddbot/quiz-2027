## 1. `tools/harness/`: the 10-client partysocket harness

- [x] 1.1 New package scaffold (`package.json`, `tsconfig.json` matching `tools/db`'s conventions, `partysocket` + `@supabase/supabase-js` deps) with a `start` script — verify `pnpm --filter harness typecheck` passes on an empty/stub entry point.
- [x] 1.2 Process orchestration: spawn `wrangler dev` for `apps/party` as a child process, wait for its "Ready on" log line (with a timeout), and a teardown routine that kills the entire process tree on both success and failure exit paths (`taskkill /F /T` on `win32`, `process.kill(-pid, "SIGTERM")` against a detached process group on POSIX — design.md D2) — verify manually: start it, confirm the Worker responds, let it exit, confirm no `workerd`/`wrangler` process remains (`tasklist`/`ps` before and after).
- [x] 1.3 Fixture setup via real RPCs (design.md D3): admin-created draft event with 3 steps (untimed/timed/untimed per design.md D4's exact configuration) and its `game_mcq` content via the service-role client (matching every other milestone's admin-authoring fixture convention); 10 real identities (6 anonymous sign-ins, 4 email/password accounts) calling real `join_event`, with P1/P5 calling `create_team` and P2–P4/P6–P7 calling `join_team` — verify by querying `participant`/`team` afterward and confirming the roster matches design.md D4 exactly (10 participants once P10 joins, 2 teams of the right sizes).
- [x] 1.4 The scripted run over real `partysocket` connections: admin claims control, starts, and per step locks/reveals/shows the leaderboard before advancing (ending after step 3); each client submits per design.md D4's exact script, with P1's step-2 answer sent-and-acknowledged before any other client submits for that step (design.md D4/Risks — avoids relying on network-timing luck for "fastest"); P10 joins (via `join_event`, then connects) only after step 1's reveal — verify the run completes without any command being rejected (no `error` message of any kind from the server during the scripted sequence).
- [x] 1.5 Hand-calculated assertions (design.md D4, computed independently in the harness's own source — never by calling `scoreStep`/`rankByTotal`): after `mc:end`, read `event_final_participant`/`event_final_team` from Postgres and assert every individual total/rank and team total/rank matches the hand-calculated table in design.md exactly (P1=30/rank 1, P6=20/rank 2, the eight-way tie at 10/rank 3; Alpha=12/rank 1, Beta=6/rank 2) — verify this is the one assertion that would fail if scoring genuinely regressed, i.e. deliberately break something in a scratch branch (e.g. flip a correct-option id) and confirm the harness fails loudly, then revert. **Done**: temporarily set `CORRECT_OPTION = "b"` in `fixture.ts`, reran — harness exited 1 and printed all 10 mismatches (e.g. `p1: expected total=30 rank=1, got total=0 rank=6`); reverted and reran — clean pass again.
- [x] 1.6 Clear console output: a pass summary (participant/team counts, final scores, step timings) on success; a non-zero exit code and a clear failure message identifying which assertion failed on failure — verify by running the harness locally to completion and reading its own output. **Done**: confirmed both the pass path (`[harness] PASSED — ...`, exit 0) and the fail path (`[harness] FAILED — N issue(s): ...`, exit 1) above.

## 2. CI integration

- [x] 2.1 New step in `.github/workflows/ci.yml`, after the existing `Test` step, running the harness against the same already-started local Supabase instance and `apps/party/.dev.vars` (design.md D6, no duplicated setup) — verify by pushing this branch and confirming the new step runs and passes in the real GitHub Actions environment (not just locally). **Done**, but with a notable finding: pushed as PR #38; the pre-existing `Test` step failed 4 of 5 real CI attempts, every time with the identical signature (`apps/party`'s `test/eventroom.test.ts > mc:start > starts the event...` getting `{type:'error'}` instead of `{type:'state', eventStatus:'live'}` — a collision on the global "at most one live event" partial unique index). This is more frequent in real CI than the `pnpm -w test` flakiness documented in MILESTONE-13/14, and re-reading the failing run's full timeline shows every *other* workspace's test process (`packages/shared`, `tools/db`, `apps/web`) had already exited before `apps/party`'s failure occurred each time — so the earlier "colliding with `tools/db`" theory doesn't hold for these particular failures; the collision is between tests inside `apps/party`'s *own* suite, despite its existing `afterEach` cleanup (`eventroom.test.ts:281`) being correctly awaited and sequential in the code as written. Root cause not further diagnosed — out of scope for this milestone (pre-existing test infrastructure, unrelated to anything in this diff). The 5th rerun passed clean, including the new "PoC validation harness" step itself; not treated as evidence the underlying flake is fixed.

## 3. Playwright e2e (local/manual only — design.md D1)

- [x] 3.1 Add `@playwright/test` as an `apps/web` devDependency, a `playwright.config.ts`, and an `e2e/` spec covering landing page → enter join code → anonymous join with consent → confirm the permanent name → reach the joined/team-lobby state, driven against real local dev servers — verify `pnpm --filter web typecheck`/`lint` stay clean with the new files present. **Done**: `apps/web/tsconfig.json`'s `include` needed `e2e`/`playwright.config.ts` added (they were silently unchecked otherwise, a false pass first time) — both genuinely clean after that fix.
- [x] 3.2 Document how to run it locally (a short section wherever `apps/web`'s own README/docs already live, or a comment block in the config if none exists — check first) since it isn't part of any CI job — verify the documented steps are the exact steps used in task 3.3. **Done**: no `apps/web/README.md` exists, so documented in new `apps/web/e2e/README.md` (mirrored in a comment block at the top of `playwright.config.ts`).
- [x] 3.3 Actually run it: local Supabase + `wrangler dev` (apps/party) + the web dev server all running together, execute the spec, and record the real outcome — do not assume it passes; if it fails, fix the underlying issue or the spec (whichever is wrong) before proceeding. **Done**: first two runs failed for real reasons — (1) `apps/web/.env.local` was missing `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` (only `VITE_PARTY_HOST` was set), so the app never rendered; (2) the generated fixture join code was 7 characters against the landing page's real `maxLength={6}` input, silently truncating. Fixed both (filled `.env.local` from the local Supabase stack; `global-setup.ts` now generates exactly 6 chars) — final run: `1 passed (6.4s)`.

## 4. PoC validation report

- [x] 4.1 Write `POC_VALIDATION_REPORT.md` at the repo root (design.md, structure per proposal.md's G-1..G-6 breakdown) — G-1 cites `eventroom.test.ts`'s `"propagation timing (FR-031 acceptance)"` suite (quote its actual assertion) plus this milestone's own harness run; G-2 cites the harness's successful unattended run and task 3.3's Playwright result; G-3 cites task 1.5's passing hand-calculated assertions; G-4 cites `event-room-core/tasks.md` task 5.4 and `resilience-recovery-hardening/tasks.md` task 5.1 verbatim (re-read both before quoting — do not paraphrase from memory); G-5 is left as an explicit open question for the owner (design.md D5) — verify every citation in the report actually matches what the cited file currently says, by re-reading each source at write time, not from earlier context. **Done**: re-read `eventroom.test.ts` (quoted `expect(elapsedMs).toBeLessThan(2000)` verbatim), `event-room-core/tasks.md` task 5.4 and `resilience-recovery-hardening/tasks.md` task 5.1 (both quoted verbatim) at write time.
- [ ] 4.2 Ask the owner directly for G-5's confirmation (real Cloudflare/Supabase/Vercel billing dashboards checked, €0 spend) and record their answer in the report — this is a close-out step, not something to do unprompted mid-implementation.

## 5. Verification and traceability

- [x] 5.1 Full workspace check green: `pnpm -w typecheck`, `pnpm -w lint`, `pnpm -w build`, `pnpm -w test` (fresh local `supabase db reset` first) — verify all pass. The new harness package is exercised separately (task 1.6/2.1), not folded into `pnpm -w test`'s existing fan-out — it needs a real spawned server, unlike every other workspace's test suite. **Done**: fresh `supabase db reset`, then `pnpm -w typecheck`/`lint`/`build` all clean (all 6 workspaces including the new `tools/harness`). Per the established pattern (known `pnpm -w test` parallel-workspace flakiness, unfixed/out of scope, documented since MILESTONE-13), tests run sequentially per package instead of trusting one `pnpm -w test`: `packages/shared` 26/26, `tools/db` 96/96, `apps/web` 179/179, `apps/party` 83/83 — unchanged from MILESTONE-14's baseline, nothing regressed. `pnpm check:shared-isolation` also clean. `tools/harness` itself (not part of this fan-out) re-run once more against the same fresh reset: `[harness] PASSED`.
- [x] 5.2 FR traceability table:

  | FR / Goal | Requirement (SPEC.md) | Verified by |
  |---|---|---|
  | FR-087 | Automated harness, 10 clients, hand-calculated score assertions, repeatable in CI | `tools/harness/` (tasks 1.1–1.6); CI step (task 2.1) |
  | G-1 | Sync flow <2s across 10 connections | Existing `eventroom.test.ts` propagation-timing suite + harness run (report §G-1) |
  | G-2 | Full happy path runs unattended | Harness's own run (report §G-2); Playwright spec (task 3.3) |
  | G-3 | Scoring correctness | Harness's hand-calculated assertions (task 1.5) |
  | G-4 | Basic resilience | Cited MILESTONE-05/14 manual verifications (report §G-4) — not re-tested here |
  | G-5 | Zero cost | Owner confirmation (task 4.2) — not code-verifiable |
  | G-6 | Decision-ready | The report itself (task 4.1) |

  No FR/Goal is unaddressed.

## 6. Close-out

- [ ] 6.1 No production-touching change this milestone (the harness/report/e2e spec are all dev/CI tooling — nothing pushes to production Supabase or the deployed Worker). Confirm the new CI step is green on `main` after merge. Then present the finished `POC_VALIDATION_REPORT.md` to the owner and get their G-5 answer if task 4.2 didn't already capture it.
