/** Admin console surface (French only). Placeholder shell for MILESTONE-01. */
export function AdminRoute() {
  return (
    <main className="mx-auto flex min-h-dvh max-w-3xl flex-col gap-4 p-6">
      <h1 className="text-2xl font-semibold">Console d&apos;administration</h1>
      <p className="text-muted-foreground">
        Placeholder — l&apos;authentification et l&apos;édition d&apos;événements arrivent avec les
        prochains jalons.
      </p>
    </main>
  );
}
